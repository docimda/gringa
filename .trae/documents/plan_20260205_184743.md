Executado o plano para adicionar funcionalidade de descontos e promoções com escassez:

### 1. Banco de Dados (Migração Criada)
- Criada a migração `20260205000000_add_product_discounts.sql` que adiciona:
  - `products`: campos `discount_percentage` e `discount_expires_at`.
  - `order_items`: campo `discount_percentage` para registrar o desconto no momento da compra.
  - `orders`: campo `total_discount` para somar a economia total do pedido.

### 2. Tipos e Interfaces
- Atualizada interface `Product` com os novos campos de desconto.
- Atualizada interface `Order` com o campo de desconto total.

### 3. Painel Administrativo (AdminProducts)
- Adicionado campo "Desconto (%)" abaixo do preço.
- Adicionado campo "Validade do Desconto" (data).
- Adicionado campo visual "Preço Final" que calcula automaticamente o valor com desconto.
- Lógica de salvamento atualizada para persistir esses dados.

### 4. Interface do Usuário (ProductCard)
- **Preço Riscado**: Agora exibe o preço original riscado quando há um desconto ativo.
- **Preço Promocional**: Exibe o novo preço com destaque (verde).
- **Badge de Desconto**: Mostra a porcentagem de desconto (ex: "-15%").
- **Escassez/Urgência**: Se houver data de validade, exibe "Expira: DD/MM/AAAA" para criar senso de urgência.

### 5. Lógica de Pedidos (OrderService)
- O cálculo do pedido agora considera os descontos ativos.
- O preço unitário salvo no item do pedido é o preço **já com desconto**.
- O percentual de desconto aplicado é salvo em cada item para histórico.
- O desconto total do pedido é calculado e salvo no registro principal da venda.

A aplicação agora suporta campanhas de desconto com data de validade, refletindo visualmente para o cliente e registrando corretamente financeiramente.