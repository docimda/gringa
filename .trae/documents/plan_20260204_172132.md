O problema foi identificado: os dados do frete (bairro, cidade e valor) existem na interface do código (`Order` type) mas **não estão sendo salvos no banco de dados** nem recuperados. Por isso, quando você seleciona "Centro" no checkout, essa informação se perde.

Para corrigir isso de forma definitiva, faremos o seguinte:

### 1. Atualização do Banco de Dados (Migração)
Criarei um novo arquivo de migração (`supabase/migrations/20260204000002_add_shipping_info_to_orders.sql`) para adicionar as colunas necessárias na tabela `orders`:
*   `shipping_city` (Texto)
*   `shipping_neighborhood` (Texto)
*   `shipping_cost` (Decimal/Numérico)

### 2. Atualização do Serviço de Pedidos (`orderService.ts`)
Modificarei o serviço para lidar com esses novos campos:
*   **`createOrder`**: Receber o objeto `shippingRate` e salvar os valores (`price`, `city`, `neighborhood`) nas novas colunas.
*   **`getOrders`**: Ler essas colunas do banco e reconstruir o objeto `shippingRate` para que o modal possa exibi-lo corretamente.

### 3. Atualização do Checkout (`CheckoutPage.tsx`)
Ajustarei a chamada `createOrder` para garantir que a taxa de entrega selecionada no carrinho (`shippingRate`) seja enviada para o serviço de criação de pedido.

---
**Resultado esperado:**
Após essas alterações, novos pedidos terão os dados de frete salvos corretamente. Para pedidos antigos (#8), os dados continuarão ausentes no banco, mas o sistema estará pronto para os novos. Se desejar, posso incluir um comando SQL manual para atualizar pedidos antigos se você tiver esses dados.